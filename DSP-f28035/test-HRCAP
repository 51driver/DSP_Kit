
//Note:
f28035-HRCAP 模块捕获脉冲计数篇：

1. 使用f28035-HRCAP 模块捕获脉冲并进行计数；
方法：
1. 配置 f28035-HRCAP 模块，
2. 采用主动轮询的方式进行脉冲个数查询，通过比较cap值的变化，判断是否有脉冲；
3. 因为f28035-HRCAP 模块有二级缓存，所以当脉冲频率大于采样周期时，每个采样周期内可捕获2个脉冲；
4. 如果采样频率低于二分之一脉冲频率，采样数据会不准确；

